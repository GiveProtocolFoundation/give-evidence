/**
 * Hello-world entry point for give-evidence.
 *
 * This file exists to give the v0 stack (Node + TypeScript + Vitest + Biome)
 * something concrete to lint, type-check, and test against. It will be
 * replaced by real code once the v0 data model lands.
 */
export function hello(name = "world"): string {
  return `Hello, ${name}!`;
}
